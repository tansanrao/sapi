import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sapi-gateway',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
